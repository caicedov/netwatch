/**
 * App Module
 *
 * Root NestJS module that imports all feature modules.
 */

export class AppModule {
  // TODO: Import feature modules (auth, players, computers, hacks, etc.)
  // TODO: Configure global providers (database, logging, etc.)
}
